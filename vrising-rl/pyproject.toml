[project]
name = "vrising-rl"
version = "0.1.0"
description = "Reinforcement learning agent for V Rising via a BepInEx bridge mod"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
